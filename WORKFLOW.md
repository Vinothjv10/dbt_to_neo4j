│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
6│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
7│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
^│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
&│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
%│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
%│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
%│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
%│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
?│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
<│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
!│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Z│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
z│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
z│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Z│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
z│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Z│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
\│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
6│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
7│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
&│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
z│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
9│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
$│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
$│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
K│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
"│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
0│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
3│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
X│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
V│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
;│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
1│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
;│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
;│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
;│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
;│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
>│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
;│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
{│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
}│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
+│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
/│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
v│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
:│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
,│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
[│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
]│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
*│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
=│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
#│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
G│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
F│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
-│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
2│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
R│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
Q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
L│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
q│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
x│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
D│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
C│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
H│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
E│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
.│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
5│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
b│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
B│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
�│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
w│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
k│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
8│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
M│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
A│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
P│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
T│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
U│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
'│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
y│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
4│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
S│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
p│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
9│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
(│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
j│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
)│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
W│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
J│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
O│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
I│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
N│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
d│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
g│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
`│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
h│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
a│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
s│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
i│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
e│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
t│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
f│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
r│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
_│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
c│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
o│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
l│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
u│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
m│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
n│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
 │  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
|│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │

│  │ 8. push-lineage    ──▶  read YAML → create Neo4j nodes/edges │  │
│  │ 9. generate-joins  ──▶  build SQL JOINs from column lineage  │  │
│  │ 10. verify         ──▶  run Cypher counts against Neo4j      │  │
